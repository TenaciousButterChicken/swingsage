# Windows production dependencies — CUDA 12.8 + Blackwell sm_120 stack.
# REQUIRES: NVIDIA driver 570+, Python 3.12+, CUDA 12.8 runtime.
#
# PyTorch must be installed FIRST and SEPARATELY from this file:
#
#   pip install torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# Then: pip install -r requirements-prod-windows.txt
#
# If torch.cuda.is_available() returns False or you hit
# "no kernel image is available for execution on the device" on RTX 5080,
# fall back to requirements-prod-windows-fallback.txt — see HANDOFF.md.

-r requirements-base.txt

# torch / torchvision / torchaudio installed separately (see header).

# Image / video
opencv-python==4.11.0.86
numpy==2.1.3
av==13.1.0  # PyAV for fast video decode

# Phase 2: 2D pose (RTMPose via MMPose)
# Versions DELIBERATELY UNPINNED here — install order is tricky on Windows.
# Follow HANDOFF.md "MMPose install" section step-by-step.
# mmengine>=0.10.5
# mmcv>=2.1.0
# mmpose>=1.3.2
# mmdet>=3.2.0

# Phase 3: 4D-Humans
# Install from source per upstream README; not pip-installable as of last check.

# Phase 4: Coaching LLM client
ollama==0.4.4

# Test (also useful on Windows for sanity)
pytest==8.4.1
